import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
      padding: 10,
      backgroundColor: '#fff',
    },
    description: {
      fontSize: 16,
      color: '#000',
    },
  });

export default styles;